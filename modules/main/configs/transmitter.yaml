name: transmitter
image:
  version: v0.0.3
repeaterSeconds: "1"
callTimeout: "3"

namespace:
  name: transmitter
  create: true

transmitterNetworkPolicy:
  receiverLabel: receiver
  create: true
  namespace:
    name: receiver